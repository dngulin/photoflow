import { ImageGrid, ImageGridItem } from "image-grid.slint";

export component PhotoFlowApp inherits Window {
    background: black;
    forward-focus: focus-scope;

    in property <int> item-count <=> grid.item-count;
    in property <[ImageGridItem]> grid-model <=> grid.model;
    pure callback set-grid-visible-range <=> grid.set-visible-range;

    grid := ImageGrid { }

    focus-scope := FocusScope {
        key-pressed(event) => {
            if (event.text == Key.RightArrow) {
                grid.mv-selection-right();
            } else if (event.text == Key.LeftArrow) {
                grid.mv-selection-left();
            } else if (event.text == Key.UpArrow) {
                grid.mv-selection-up();
            } else if (event.text == Key.DownArrow) {
                grid.mv-selection-down();
            }
            accept
        }
    }
}
